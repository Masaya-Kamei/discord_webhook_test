name: pull_request

on:
  pull_request


jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: test
        run: |
          echo ${{ console.log(github.event) }}

